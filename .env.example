# V2 browser voice mode does not require API keys.
# Fill these only if you later switch STT/TTS adapters to external providers.

STT_PROVIDER=browser
TTS_PROVIDER=browser

# V3: Advanced provider support for scalable, production-like demos
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
